import 'package:flutter/material.dart';

class TPMSModel extends StatelessWidget {
  const TPMSModel({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TPMS'),
      ),
      body: Center(
        child: Text('TPMS'),
      ),
    );
  }
}